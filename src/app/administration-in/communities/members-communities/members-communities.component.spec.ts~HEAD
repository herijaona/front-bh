import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MembersCommunitiesComponent } from './members-communities.component';

describe('MembersCommunitiesComponent', () => {
  let component: MembersCommunitiesComponent;
  let fixture: ComponentFixture<MembersCommunitiesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MembersCommunitiesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MembersCommunitiesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
