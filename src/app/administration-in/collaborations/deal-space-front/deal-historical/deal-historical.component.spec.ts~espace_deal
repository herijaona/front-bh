import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DealHistoricalComponent } from './deal-historical.component';

describe('DealHistoricalComponent', () => {
  let component: DealHistoricalComponent;
  let fixture: ComponentFixture<DealHistoricalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DealHistoricalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DealHistoricalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
